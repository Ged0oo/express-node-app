services:
  app:  
    build:
      context: .
      args: 
        - NODE_ENV=development
    volumes:
      - ./src:/app/src:ro
    environment:
      - PORT=4000
      - NODE_ENV=development 
    command: npm run start-dev
